import SwiperComponent from "@/components/slider/SwiperComponent";
import { Stack } from "@mui/material";

const HeroSection: React.FC = () => {
  return (
    <Stack p="0 3rem 0 3rem">
      <SwiperComponent />
    </Stack>
  );
};

export default HeroSection;
